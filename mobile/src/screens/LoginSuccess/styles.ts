import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    alignItems: 'center',
    justifyContent: 'center',
    backgroundColor: '#00411F',
  },

  content: {
    width: 100,
    height: 100,
    backgroundColor: '#00B44B',
    borderRadius: 100,
    alignItems: 'center',
    justifyContent: 'center',
  }
});